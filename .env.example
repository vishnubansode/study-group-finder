# Example environment variables for local development
# Copy this file to `.env` and fill in REAL values. Never commit secrets.

# ⚠️  SECURITY WARNING: 
# - Replace ALL placeholder values with strong, unique secrets
# - Never commit the actual .env file to version control
# - Use different secrets for dev/staging/production environments

# Database Configuration (Local Development)
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/studygroup
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=your_mysql_password_here

# JWT Secret (CRITICAL: Use a strong 256-bit secret)
# Generate with: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_STRONG_JWT_SECRET_256_BITS_MIN

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Frontend Environment Variables
VITE_API_URL=http://localhost:8080/api
VITE_SOCKET_URL=http://localhost:8080

# Docker Environment Variables (for docker-compose)
MYSQL_ROOT_PASSWORD=REPLACE_WITH_STRONG_ROOT_PASSWORD
MYSQL_DATABASE=studygroup
MYSQL_USER=studygroup_user
MYSQL_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
